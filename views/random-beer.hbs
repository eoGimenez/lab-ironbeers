{{#each random}}
<div>
    <img class="imgRandom" src="{{image_url}}" alt="{{name}}">
    <p>{{name}}</p>
    <p>{{description}}</p>
    <p>{{tagline}}</p>
    <p>{{brewers_tips}}</p>
    <ul>
        {{#each food_pairing}}
        <li>{{this}}</li>
        {{/each}}
    </ul>
</div>
{{/each}}